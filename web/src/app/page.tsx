import { SiteLanding } from "@/components/site/SiteLanding";

// Marketing site — the public landing at the root URL. The product itself lives
// at /app (see app/app/page.tsx).
export default function Home() {
  return <SiteLanding />;
}
